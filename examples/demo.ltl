// examples/demo.ltl — runnable demo for ltl_changelog_lint

import std::io
use ltl_changelog_lint as lib

fn main() {
    io::println("ltl_changelog_lint demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}